# Google Apps Script Web App URL that appends cart checkout leads (name, phone,
# order summary) to a Google Sheet. See docs/21_GOOGLE_SHEETS_LEAD_INTEGRATION.md
# for how to create and deploy it. Must be NEXT_PUBLIC_ — it's called directly
# from the browser when a customer checks out via WhatsApp.
NEXT_PUBLIC_ORDER_SHEET_WEBHOOK_URL=
